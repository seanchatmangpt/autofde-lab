# autofde-lab v26.9.16 — RFC Closure Contract

Status: DRAFT INTEGRATION/QUALIFICATION PR.

## Canonical Jira tickets

- A2A-2605 through A2A-2612 — cross-repo integration/qualification consumer

## RFC ownership

This repo owns the integration-lab proof that the independently implemented pieces compose as one bounded autonomous workflow rather than ten locally-green repositories.

## Required closure

1. Consume exact subjects from ash_a2a, ggen, bcinr, unrdf, ash_r2rml, ggen_igniter, wasm4pm, affidavit and the local-model provider.
2. Exercise the end-to-end path:
   `candidate -> admission -> work class -> bounded allocation -> deterministic/local/deferred/frontier route -> candidate result -> admission -> manufacture -> authority -> BRCE/CommandBus -> receipt -> feedback -> reusable machine experience`.
3. Prove at least one KNOWN request performs zero LLM calls.
4. Prove at least one bounded local UNKNOWN uses the local-model tier.
5. Prove at least one deferred UNKNOWN executes through the AtomVM compute-cell profile.
6. Prove frontier inference occurs only after explicit local insufficiency/allocation evidence.
7. Prove successful UNKNOWN compile-back causes a repeated equivalent request to take the future KNOWN/zero-LLM path.
8. Preserve explicit standing boundaries for local run, CI, merge, deployment and ALIVE.

## Chicago falsifiers

- any provider/model/worker grants itself authority or budget;
- same known-class request still invokes a model after qualified compile-back;
- deferred worker output bypasses semantic admission;
- one required repo subject changes without invalidating the integration receipt;
- replay performs a second consequence;
- failure of a local worker silently becomes frontier spend without an allocation event;
- aggregate test reports green when one required lower-level subject is absent.

## Definition of done

An exact-head, repository-native Chicago court closes all normative RFC paths listed above against exact cross-repo subjects. This establishes integration evidence only; production/runtime ALIVE remains a separate standing claim requiring its own observed deployment evidence.

# v26.9.22 — pointer

Per this repo's docs/jira/vX practice, the v26.9.22 work orders live in the
xaas v26.9.22 graph with `repository=seanchatmangpt/autofde-lab`, matching how
SJ-006 was hosted (see `~/xaas/docs/sjira/v26.9.22/` once published, and this
repo's `receipts/v26.9.22/` for the per-work-order execution receipts).

import FailureWorkbench from "./FailureWorkbench";

export default function Home() {
  return (
    <main>
      <header>
        <p className="eyebrow">WD · Semantic Failure Analysis</p>
        <h1>Every solved failure becomes executable prior art.</h1>
        <p className="lede">
          Next.js is a projection surface. FastAPI and sJira own application and semantic admission.
        </p>
      </header>
      <FailureWorkbench />
      <section className="boundary">
        <strong>Authority ceiling</strong>
        <code>Next.js → FastAPI → sJira → SELECT / CONSTRUCT only</code>
      </section>
    </main>
  );
}

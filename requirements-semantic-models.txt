# Post-Chapman semantic-model manufacturing.
# Install the project itself first, then this optional stack:
#   uv pip install -e .
#   uv pip install -r requirements-semantic-models.txt
# Domain truth remains in ontology/SHACL; these packages manufacture or rank candidates only.

pydantic>=2
dspy
tpot>=1.1
scikit-learn>=1.6
rdflib
pyshacl

# Optional student-model manufacture. The pipeline imports these lazily.
transformers
accelerate
datasets
peft
trl

"""autofde_lab_planner: this worktree checkout contains only the new `scanner` package.

The abandoned 14-function enumeration (`models.py`, `engine.py`, `detectors/`,
`remediators/`) referenced by this task is uncommitted work-in-progress in a
sibling working tree and is not present here (git worktrees only share
committed history) -- untouched, per the task's constraint, by construction.
"""
